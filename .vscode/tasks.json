{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_mandatory",
            "type": "shell",
            "command": "make -C ${workspaceFolder}/minishell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
		{
            "label": "build_bouns",
            "type": "shell",
            "command": "make bonus -C ${workspaceFolder}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}